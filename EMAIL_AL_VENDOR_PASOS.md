# Email al vendor (aprobación/rechazo)

Usamos un **email relay** en Netlify con **Resend**. No hace falta EmailJS.

Sigue los pasos en **`EMAIL_RELAY_PASOS.md`** (cuenta Resend → variable `RESEND_API_KEY` en Netlify → redeploy).
